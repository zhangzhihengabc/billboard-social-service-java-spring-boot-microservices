package com.billboard.social.group.entity.enums;

public enum GroupType {
    PUBLIC,     // Anyone can see and join
    CLOSED,     // Anyone can see but must request to join
    PRIVATE,    // Only members can see content
    SECRET      // Group is hidden from search
}
